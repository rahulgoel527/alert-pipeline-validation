# Hostnames — localhost for local runs; overridden per-service in docker-compose.yml for Docker
REDIS_HOST=localhost
POSTGRES_HOST=localhost
ELASTICSEARCH_HOST=localhost
API_HOST=localhost

# Shared config — identical in both modes
POSTGRES_DB=alert_ledger
POSTGRES_USER=ledger
POSTGRES_PASSWORD=ledger123
ES_INDEX=security_alerts
